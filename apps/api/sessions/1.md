To address your request to fix the Tiptap editor in the domoreai project (assuming you meant "accept numbers" instead of "except numbers"), I'll orchestrate a coding agent (using Codex) to handle this task. This will be executed in the specified target directory: `/home/guy/mono`.

I'll use the terminal tool with the required parameters:
- **command**: The full command to run Codex, including your prompt and a wake trigger for auto-notification when the task completes.
- **pty**: `true` (as required for interactive terminal applications).
- **workdir**: `/home/guy/mono` (the target project directory).
- **background**: `true` (to run the process in the background and return a session ID for monitoring).

The prompt for the coding agent will be:  
"Fix the Tiptap editor in the domoreai project to accept numbers (e.g., allow numeric input or handling). When completely finished, run: openclaw system event --text 'Done: Fixed Tiptap editor to accept numbers' --mode now'"

This ensures that once the agent finishes, the system will be notified automatically.

Now, invoking the terminal tool:

Use the terminal tool with:  
- command: `codex exec 'Fix the Tiptap editor in the domoreai project to accept numbers (e.g., allow numeric input or handling). When completely finished, run: openclaw system event --text \"Done: Fixed Tiptap editor to accept numbers\" --mode now'`  
- pty: true  
- workdir: "/home/guy/mono"  
- background: true  

This will start the task in the background and return a session ID. You can use that session ID to monitor progress if needed. If you have any follow-up instructions, let me know!